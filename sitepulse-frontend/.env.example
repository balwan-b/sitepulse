# Local dev only: the frontend talks directly to the backend on this URL.
VITE_BACKEND_BASE_URL=http://localhost:4000



# SENTRY_DSN — optional for Sentry.
# RATE_LIMIT_MAX — override rate limiter max (default 120).
# DASHBOARD_CACHE_TTL_MS — dashboard cache TTL (default 15000).
# ALLOW_DESTRUCTIVE_SEED=1 — required to run demo/reset-and-seed-neon.mjs.
